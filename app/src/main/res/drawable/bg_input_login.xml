<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused state -->
    <item android:state_focused="true">
        <layer-list>
            <item android:top="2dp" android:left="2dp">
                <shape android:shape="rectangle">
                    <corners android:radius="20dp" />
                    <solid android:color="#1A6200EE" />
                </shape>
            </item>
            <item android:bottom="2dp" android:right="2dp">
                <shape android:shape="rectangle">
                    <corners android:radius="20dp" />
                    <solid android:color="#FFFFFF" />
                    <stroke android:width="2dp" android:color="#6200EE" />
                </shape>
            </item>
        </layer-list>
    </item>

    <!-- Error state (Custom state or handled via code, let's use a standard one for now) -->
    <!-- We will handle colors dynamically in code for better control over "Soft" tones -->
    
    <!-- Default state -->
    <item>
        <layer-list>
            <item android:top="2dp" android:left="2dp">
                <shape android:shape="rectangle">
                    <corners android:radius="20dp" />
                    <solid android:color="#0D000000" />
                </shape>
            </item>
            <item android:bottom="2dp" android:right="2dp">
                <shape android:shape="rectangle">
                    <corners android:radius="20dp" />
                    <solid android:color="#F8F8F8" />
                    <stroke android:width="1dp" android:color="#E0E0E0" />
                </shape>
            </item>
        </layer-list>
    </item>
</selector>